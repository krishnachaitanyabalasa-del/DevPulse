version: '3.8'

services:
  devpulse-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: devpulse-backend
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - NEO4J_URI=bolt+s://db-7ba26f4a.databases.cognodb.com:7687
      - NEO4J_USERNAME=cognodb
      - NEO4J_PASSWORD=998ebf2bc471a0bf18d308e58fb94526
    restart: unless-stopped
